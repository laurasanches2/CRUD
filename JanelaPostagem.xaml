<Window x:Class="CRUD.JanelaPostagem"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Nova Postagem"
        Height="350"
        Width="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Name="GridPostagem"
              Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBox Grid.Row="0"
                     Background="LavenderBlush"
                     Padding="4"
                     FontSize="16"
                     MaxLength="140"
                     TextWrapping="Wrap"
                     MaxLines="6"
                     MinHeight="180"
                     x:Name="TbConteudo"
                     TextChanged="TbConteudo_OnTextChanged">
                <TextBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5" />
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <Label Grid.Row="1"
                   Content="0/140"
                   HorizontalAlignment="Right"
                   FontSize="11"
                   FontWeight="Light" 
                   x:Name="LblCaracteresMax"/>
        </Grid>

        <Button Grid.Row="2"
                Background="LavenderBlush"
                Foreground="Plum"
                Content="Postar"
                FontSize="20"
                FontWeight="Bold"
                Margin="8"
                Padding="8"
                Click="BtnPostar_OnClick"
                IsDefault="True"
                ToolTip="A postagem só pode ser feita se o campo for preenchido!"
                x:Name="BtnPostar">
            
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5" />
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>