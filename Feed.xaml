<Window x:Class="CRUD.Feed"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:modelos="clr-namespace:CRUD.Modelos"
        mc:Ignorable="d"
        Title="Feed" Height="600" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#E1E8ED" BorderThickness="0,0,0,1">
            <TextBlock Text="Seu Feed" VerticalAlignment="Center" HorizontalAlignment="Left"
                       Margin="20,0,0,0" FontSize="20" FontWeight="Bold" Foreground="#1A1A1A" />
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10">
            <StackPanel>
                <ItemsControl x:Name="ItemsControlfeed">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type modelos:Postagem}">
                            <Border Background="White" CornerRadius="8" Margin="0,0,0,12" Padding="15">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="10" Color="#000000" Opacity="0.05" ShadowDepth="1" />
                                </Border.Effect>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="{Binding Usuario.Nome}" FontSize="14" FontWeight="Bold"
                                                   Foreground="#1A1A1A" />
                                        <TextBlock Text="{Binding Usuario.Username, StringFormat='@{0}'}" FontSize="13"
                                                   Foreground="#657786"
                                                   Margin="6,1,0,0" />
                                        <TextBlock Text="•" FontSize="13" Foreground="#657786" Margin="6,1,0,0" />
                                        <TextBlock Text="{Binding Postado_em,StringFormat =' dd MMM = HH:mm'}"
                                                   FontSize="13" Foreground="#657786"
                                                   Margin="6,1,0,0" />
                                    </StackPanel>

                                    <TextBlock Grid.Row="1"
                                               Text="{Binding Conteudo}"
                                               FontSize="14" Foreground="#292F33" TextWrapping="Wrap" LineHeight="20"
                                               Margin="0,0,0,12" />

                                    <StackPanel Grid.Row="2" Orientation="Horizontal">
                                        <Button Content="❤️ Curtir" Background="Transparent" BorderThickness="0"
                                                Cursor="Hand"
                                                Foreground="#E0245E" FontWeight="SemiBold" FontSize="13">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Opacity" Value="0.7" />
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>